[package]
name = "kiro-rs"
version = "0.9.14"
edition = "2024"

[features]
default = ["native-tls"]
native-tls = ["reqwest/native-tls-vendored"]

[profile.release]
lto = "thin"
codegen-units = 16
strip = true

[dependencies]
axum = "0.8"
tokio = { version = "1.0", features = ["full"] }
reqwest = { version = "0.12", default-features = false, features = ["stream", "json", "socks", "rustls-tls-webpki-roots", "rustls-tls-native-roots", "http2", "system-proxy", "charset"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1.0"
http = "1.0"
httpdate = "1.0"
futures = "0.3"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1.10", features = ["v1", "v4", "fast-rng"] }
fastrand = "2"
sha2 = "0.10"
hex = "0.4"
crc = "3"           # CRC32C 计算
bytes = "1"         # 高效的字节缓冲区
tower-http = { version = "0.6", features = ["cors"] }
clap = { version = "4.5", features = ["derive"] }
urlencoding = "2"
parking_lot = "0.12"  # 高性能同步原语
# Image resize for inbound user/MCP images so AWS Q stays within request size
# limit. Pure-Rust, no C deps. PNG/JPEG/WebP/GIF only — keeps build slim.
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "webp", "gif"] }
base64 = "0.22"
thiserror = "1"
subtle = "2.6"        # 常量时间比较（防止时序攻击）
rust-embed = "8"      # 嵌入静态文件
mime_guess = "2"      # MIME 类型推断
flate2 = "1"        # tar.gz 解压（在线更新）
tar = "0.4"         # tar 解析（在线更新）
zip = { version = "2", default-features = false, features = ["deflate"] }  # zip 解压（Windows 在线更新）
# 只用无扩展的核心功能：musl 静态二进制不支持动态加载扩展，离线容器也无法
# 运行时下载；icu（SET TimeZone）与 json（read_json）扩展的静态内联又依赖
# crates.io 不发布的 cmake 源码。因此本地时区切桶在 Rust 侧预计算（hour_ts/day_ts
# 列），历史 JSONL 导入走 serde 逐行解析。
duckdb = { version = "1", features = ["bundled"] }  # usage/trace/ops 统一存储（kiro.duckdb，自带源码）
